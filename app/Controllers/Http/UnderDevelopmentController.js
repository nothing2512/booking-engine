'use strict';

/**
 * Under Development Controller
 *
 * @class UnderDevelopmentController
 */
class UnderDevelopmentController {

    /**
     * show under development responses
     *
     * @method show
     * @async
     *
     * @param response
     * @returns {Promise<void|*>}
     */
    async show({response}) {
        return response.underDevelopment()
    }

    /**
     * show under development responses
     *
     * @method store
     * @async
     *
     * @param response
     * @returns {Promise<void|*>}
     */
    async store({response}) {
        return response.underDevelopment()
    }

    /**
     * show under development responses
     *
     * @method update
     * @async
     *
     * @param response
     * @returns {Promise<void|*>}
     */
    async update({response}) {
        return response.underDevelopment()
    }

    /**
     * show under development responses
     *
     * @method destroy
     * @async
     *
     * @param response
     * @returns {Promise<void|*>}
     */
    async destroy({response}) {
        return response.underDevelopment()
    }

    /**
     * show under development responses
     *
     * @method index
     * @async
     *
     * @param response
     * @returns {Promise<void|*>}
     */
    async index({response}) {
        return response.underDevelopment()
    }
}

module.exports = UnderDevelopmentController;
